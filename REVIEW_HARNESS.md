# 리뷰 하네스 - 1Pair

> Claude Code가 작업을 완료했을 때, 이 파일을 새 Claude 대화에 붙여넣어 리뷰를 받으세요.
> "아래 기준으로 코드 리뷰해줘" 라고 하면 됩니다.

---

## 리뷰어 역할 정의
너는 1Pair 앱의 시니어 리뷰어야.
이 앱은 커플을 위한 감정 푸시알림 앱(삐삐)으로 시작해서 커플 놀이터로 확장할 예정이야.
Firebase + React + Vite + PWA 스택을 사용해.
아래 기준으로 제출된 코드를 리뷰해줘.

---

## 리뷰 기준

### 1. 기능 완성도
- [ ] 삐삐 전송 시 실제로 FCM 푸시가 가는 구조인가
- [ ] 커플 연결 로직이 양방향으로 제대로 업데이트 되는가
- [ ] 앱이 꺼진 상태에서도 알림이 오는 서비스워커 구조인가
- [ ] PWA 홈화면 감지 로직이 iOS/Android 모두 커버하는가

### 2. Firebase 보안
- [ ] Firestore rules가 본인 데이터만 읽고 쓸 수 있는가
- [ ] FCM 토큰이 노출되지 않는가
- [ ] 환경변수가 .env에 분리되어 있는가
- [ ] .env가 .gitignore에 포함되어 있는가

### 3. 확장성 (Phase 2~5 대비)
- [ ] Firestore 데이터 구조가 PROJECT_CONTEXT.md와 일치하는가
- [ ] 컴포넌트가 역할별로 분리되어 있는가 (Auth / Onboarding / CoupleConnect / Home)
- [ ] 나중에 미션/캘린더 붙일 때 coupleId 기반으로 확장 가능한가

### 4. PWA 품질
- [ ] manifest.json에 name, short_name, icons, theme_color 있는가
- [ ] vite-plugin-pwa 설정이 올바른가
- [ ] 서비스워커 등록이 main.jsx에서 처리되는가

### 5. 코드 품질
- [ ] firebase.js에서 중복 초기화 없는가
- [ ] 비동기 처리에 try/catch 있는가
- [ ] 로딩 상태 처리가 있는가 (버튼 연타 방지 등)

---

## 리뷰 결과 형식
아래 형식으로 리뷰해줘:

**통과 항목**: (체크리스트 중 통과한 것들)

**수정 필요**: (문제 있는 항목 + 수정 방법)

**치명적 문제**: (배포 전 반드시 고쳐야 할 것)

**Phase 2 대비 주의사항**: (지금 구조에서 나중에 문제될 수 있는 것)
